PORT=3000

# Optional: set this to your app domain.
ALLOWED_ORIGIN=*

# SMTP (SMTP_USER is also used as the From address)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=example-user
SMTP_PASS=example-password
