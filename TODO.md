# SplitEase Server — TODO

- [ ] Re-enable `MAIL_API_KEY` / `x-api-key` auth on `POST /send-mail` once the mail flow is stable (was removed temporarily for easier testing).
- [ ] Optional: set matching `SEND_EMAIL_HOOK_SECRET` on Vercel + Supabase Auth Hooks for signature verification.
